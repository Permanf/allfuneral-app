export interface IContactProps {
    contact:{
        id: string;
        firstname: string;
        lastname: string;
        phone: string;
        email: string;
    }
}

export interface FormData{
    name: string;
    phone: string;
    email: string;
}